server:
  port: 8082

spring:
  application:
    name: library-service
  datasource:
    url: jdbc:mysql://localhost:33061/library-service
    username: root
    password: MySQL@8118512
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

feign:
  hystrix:
    enabled: true

logging:
  level:
    org.springframework: DEBUG
    com.example: DEBUG